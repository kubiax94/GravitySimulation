# Copilot Instructions

## Wytyczne projektu
- User wants engine-level generic solutions rather than scene-specific hacks; prefers extracting high-count simulated/rendered objects into a particle-system-like architecture handled as a single transform on CPU.